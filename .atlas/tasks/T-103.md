---
id: T-103
type: task
status: todo
priority: medium
parent_spec: 001-player-service.md
---

# Implement Player Endpoint

Create the GET /api/player/{id} endpoint to retrieve player profile data.

## Sub-tasks:

- [x] **Create PlayerService**: Create `Services/PlayerService.cs` with a method to retrieve player by ID, returning null if not found
- [x] **Register PlayerService**: Register `PlayerService` in dependency injection in `Program.cs`
- [x] **Create PlayerController**: Create `Controllers/PlayerController.cs` with `[ApiController]` and `[Route("api/[controller]")]` attributes
- [x] **Implement GET endpoint**: Add `GET {id}` action that returns player data in the specified JSON format, or 404 if not found
- [x] **Add controllers to pipeline**: Add `builder.Services.AddControllers()` and `app.MapControllers()` to `Program.cs`
- [x] **Test endpoint**: Verify the endpoint returns correct response format using the `.http` file or curl
